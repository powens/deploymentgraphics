# Board size and the draw toggles. All styling lives in theme.yml.
# What an absent `draw` means is per toggle, and src/layers.ts states it
# beside the layer it gates.
size:
  width: 60
  height: 44

half_way_lines:
  draw: true

territory:
  draw: true

grid:
  draw: true
